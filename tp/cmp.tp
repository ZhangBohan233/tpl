
fn main() int {
    var c: *char = malloc(7);
    var d: char[] = "qwerty";
    mem_copy(c, d, 6);

    var e: *char = malloc(4);
    var f: char[] = "abc";
    mem_copy(e, f, 3);

    var args: **char = malloc(16);
    args[0] = c;
    args[1] = e;

    var ee: char = args[0][0];
    printf("%c\n", ee);

    return 0;
}
