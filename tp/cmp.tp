struct A {
    var a: int;
    var b: float;
    var c: int;
}


fn main() int {
    var b: *A = malloc(sizeof(A));
    printf("%d\n", b);
    b..a = 5;
    b..b = 66.4;
    b..c = 3;

    var c: float = b..b;

    free(b);

    var arr: int[2];
    arr[0] = 32;
    arr[1] = 7;

    var d: *int = malloc(16);
    mem_copy(d, arr, 16);

    return int(c);
}
