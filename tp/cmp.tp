fn add(a: int, b: int) int {
    return a + b;
}

fn main() int {
    var t0: int = clock();
    var a: int = add(3, 4);
    for i := 0; i < a; ++i {
        printf("%d\n", i);
    }
    var t1: int = clock();
    return t1 - t0;
}
