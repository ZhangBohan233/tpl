fn fib(n: int) int {
    if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

fn main() int {
    const t0: int = clock();
    var f: int = fib(20);
    const t1: int = clock();

    printf("fib time %d\n", t1 - t0);

    return f;
}

